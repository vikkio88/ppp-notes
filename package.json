{
  "name": "ppp-astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "bunx --bun astro dev",
    "build": "bunx --bun astro build",
    "preview": "bunx --bun astro preview",
    "astro": "bunx --bun astro",
    "build:clean": "bun scripts/clean.ts",
    "feed:update": "bun scripts/parse.ts",
    "site:update-deploy": "bun build:clean && bun feed:update && bun run build && bun site:deploy",
    "site:deploy": "bunx surge dist --domain ppp-notes.surge.sh"
  },
  "dependencies": {
    "@astrojs/react": "^4.1.5",
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3",
    "astro": "^5.1.7",
    "date-fns": "^4.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "rss-parser": "^3.13.0"
  },
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
